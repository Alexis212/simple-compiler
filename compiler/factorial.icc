sea mut facto : entero = 1;
sea mut i, num : entero;


fn principal()
{
        imprimeln("Dame un numero: ");
        leer(num);

        para i en 1..=num
        {
                facto = facto * i; 
        }
        imprimeln!("Factorial(", i-1, ") = ", facto);
}
