sea mut x:entero = 7;

fn principal()
{
        si x > 10
        {
                imprimeln(x); 
        }
        si x > 20
        {
                imprimeln(x);
        }
        sino
        {
                imprimeln(x);
        }

        imprimeln!("Adios");
}
