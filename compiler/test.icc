fn main()
{
        sea vec[2]: entero = {3, 10};
        sea mut max: entero;
        
        si (a > b)
        {
                max = a;
        }
        sino
        {
                max = b;
        }

        imprimeln(max);
}
